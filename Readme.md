### Use git subtree to update folders.

## Updating folders
git subtree add --prefix=foldername https://github.com/username/repo.git main --squash
<!-- squash to make it only one commit instead of a merge commit -->

